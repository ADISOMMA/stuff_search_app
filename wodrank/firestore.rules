rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regola semplice per l'MVP:
    // Chiunque sia loggato (request.auth != null) può leggere e scrivere ovunque.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}